const LoadingSpinner = () => (
  <div className="spinner-border text-primary" role="status">
    <span className="sr-only" style={{ visibility: 'hidden' }}>
      Loading...
    </span>
  </div>
);

export default LoadingSpinner;
